# Copy to infrastructure/.env for local non-demo environments. Do not commit .env.
POSTGRES_DB=securetrack
POSTGRES_USER=securetrack
POSTGRES_PASSWORD=replace-with-a-unique-long-password
JWT_SECRET=replace-with-a-random-32-byte-or-longer-secret
CORS_ORIGIN=http://localhost:3000
RUN_MIGRATIONS=true
